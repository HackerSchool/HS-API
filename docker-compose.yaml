services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ${DATABASE_PATH}:/app/${DATABASE_PATH}
      - ${TAGS_PATH}:/app/${TAGS_PATH}
      - ${PHOTOS_DIR}:/app/${PHOTOS_DIR}
      - $(LOGS_PATH}:/app/${LOGS_PATH}