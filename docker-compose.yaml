services:
  redis:
    image: redis:6.2-alpine
    restart: unless-stopped 
  backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./resources:/hs-api/resources/
    env_file: ".env"
    depends_on:
      - "redis"