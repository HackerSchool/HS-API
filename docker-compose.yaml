services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ${DATABASE_PATH}:/app/${DATABASE_PATH}
      - ${TAGS_PATH}:/app/${TAGS_PATH}
      - ${LOGOS_PATH}:/app/${LOGOS_PATH}